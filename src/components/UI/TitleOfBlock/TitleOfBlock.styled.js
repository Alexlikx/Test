import styled from "styled-components";
import { motion } from 'framer-motion';

const TittleOfBlockStyled = styled(motion.h3)`
    margin: 40px auto;
    opacity: 0.8;
    font-weight: 700;
    text-align: center;
    font-size: 34px;
    color: rgb(0, 0, 50);
`;

export default TittleOfBlockStyled;